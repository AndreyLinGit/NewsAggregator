@model NewsAggregator.Models.Account.UserViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Личная страница @Model.Login</h1>
<img src="@Model.ImagePath" style="height:100px;width:100px">
<div>
    <a class="btn btn-primary" asp-route-id="@Model.Id" asp-action="CreateImage">Сменить изображение</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}